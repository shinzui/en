# Authentication perimeter suite

This opt-in suite requires an `en-server` configured with separate read-write and
read-only API keys. Exact startup and invocation instructions are completed in
Milestone 4 of the active ExecPlan.
